query IssueRelations($id: String!) {
  issue(id: $id) {
    id
    identifier
    relations {
      nodes {
        type
        relatedIssue {
          id
          identifier
          title
          state {
            id
            name
            type
          }
        }
      }
    }
  }
}