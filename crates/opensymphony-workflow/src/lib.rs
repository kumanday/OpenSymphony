//! Placeholder workflow crate until the M1 workflow loader lands.

/// Placeholder exported to keep the crate boundary explicit.
#[must_use]
pub fn crate_boundary() -> &'static str {
    "opensymphony-workflow"
}
