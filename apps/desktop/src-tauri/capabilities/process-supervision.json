{
  "identifier": "process-supervision",
  "description": "Scoped permission for local daemon process supervision stubs. opener:default grants only the minimal URL opening capability. No execute/kill permissions are active. COE-404 will implement whitelisted executables, PID tracking, and input sanitization before any execute/kill permissions are added.",
  "windows": ["main"],
  "permissions": [
    "opener:default"
  ]
}
