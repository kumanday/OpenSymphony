[project]
name = "opensymphony-openhands-server"
version = "0.1.0"
description = "Pinned local OpenHands agent-server environment for OpenSymphony."
requires-python = ">=3.11"
dependencies = [
    "openhands-agent-server==1.14.0",
]

[tool.uv]
package = false
